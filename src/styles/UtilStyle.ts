import {createGlobalStyle} from "styled-components"

export const UtilStyle = createGlobalStyle/* css */`
  .primaryBtn {
    background-color: var(--primary) !important;
    color: var(--primaryContrast) !important;
    font-weight: bold !important;
  }
`
